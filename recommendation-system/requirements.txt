# Pinned to avoid the NumPy 2.x import error with scikit-surprise.
# scikit-surprise is built against NumPy 1.x and will throw
# "ImportError: numpy.core.multiarray failed to import" if NumPy 2.x is installed.

numpy>=1.23,<2.0
pandas>=2.0,<2.3
scipy>=1.10,<2.0

matplotlib>=3.7
seaborn>=0.12

scikit-surprise>=1.1.3

# Jupyter
jupyter>=1.0
ipykernel>=6.0
